# ============================================================
# RUMA — BNB Hack: AI Trading Agent Edition
# Full dependency list for BSC autonomous trading agent
# ============================================================

# ── FastAPI / Server ──────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
sse-starlette>=2.1.0
websockets>=13.0

# ── Validation / Schemas ──────────────────────────────────────────────────────
pydantic>=2.9.0
python-dotenv>=1.0.0

# ── BNB Chain / Web3 (BSC + TWAK self-custody signing) ───────────────────────
web3>=7.4.0
eth-account>=0.13.0
eth-typing>=5.0.0
hexbytes>=1.2.0

# ── HTTP Client (CMC AI Agent Hub + TWAK API + Telegram) ─────────────────────
httpx>=0.27.0
aiohttp>=3.10.0

# ── AI / LLM Reasoning (5 parallel chains) ───────────────────────────────────
anthropic>=0.40.0

# ── CoinMarketCap AI Agent Hub (MCP + x402) ──────────────────────────────────
# httpx covers REST; websockets covers MCP streaming
# No additional package needed beyond httpx for CMC hub

# ── FAISS Memory (self-reflection plane, persisted to disk) ──────────────────
faiss-cpu>=1.9.0
numpy>=1.26.0

# ── Trading / Signal Processing ───────────────────────────────────────────────
ccxt>=4.3.0
pandas>=2.2.0

# ── Sentence Embeddings (reasoning chain vectorisation) ──────────────────────
sentence-transformers>=3.2.0

# ── Crypto / Security ────────────────────────────────────────────────────────
cryptography>=43.0.0
PyNaCl>=1.5.0

# ── Storage / DB (optional TimescaleDB) ──────────────────────────────────────
asyncpg>=0.29.0
sqlalchemy>=2.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
aiofiles>=24.1.0
python-dateutil>=2.9.0
pytz>=2024.1
ujson>=5.10.0
fastapi
python-multipart
sse-starlette
uvicorn[standard]
websockets
fastapi
pydantic
python-dotenv
python-multipart
sse-starlette
uvicorn[standard]
websockets
aiofiles
aiohttp
anthropic
asyncpg
ccxt
cryptography
eth-account
eth-typing
faiss-cpu
fastapi
hexbytes
httpx
numpy
pandas
pydantic
PyNaCl
python-dateutil
python-dotenv
python-multipart
pytz
sentence-transformers
sqlalchemy
sse-starlette
ujson
uvicorn[standard]
web3
websockets
PyNaCl
sqlalchemy
